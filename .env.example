# ─── juliaz_agents — Environment Variables ───────────────────────────────────
# Copy this file and fill in real values. Never commit actual keys.
#
# Each service reads its own .env file (not this one).
# This file is a reference showing ALL required variables across the system.

# ─── Orchestrator (orchestrator/.env) ────────────────────────────────────────
OPENAI_API_KEY=sk-proj-...
ANTHROPIC_API_KEY=sk-ant-...
COWORK_MCP_URL=http://localhost:3003
RAPHAEL_CHAT_ID=123456789              # Telegram chatId for /dev command auth

# ─── Frontend (frontend/.env.local) ─────────────────────────────────────────
OPENAI_API_KEY=sk-proj-...
ANTHROPIC_API_KEY=sk-ant-...           # Required for Claude Sonnet model option

# ─── Backend (backend/.env) ─────────────────────────────────────────────────
DATABASE_URL=postgresql://user:password@localhost:5432/juliaz

# ─── Bridge (bridge — env vars or defaults) ─────────────────────────────────
BRIDGE_PORT=3001

# ─── Cowork-MCP (cowork-mcp — env vars or defaults) ─────────────────────────
ANTHROPIC_API_KEY=sk-ant-...
COWORK_MCP_PORT=3003
CLAUDE_MODEL=claude-haiku-4-5-20251001
